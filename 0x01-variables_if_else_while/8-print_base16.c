#include <stdio.h>

/**
 * main -  Prints all the numbers of base 16
 * in lowercase
 * Return: 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 48; i < 58; i++)
	{
		putchar((i % 10 + '0')
	}
	for (i = 97; i <= 102; i++)
	{
		putchar(i);
	}
		putchar('\n');
	return (0);
}

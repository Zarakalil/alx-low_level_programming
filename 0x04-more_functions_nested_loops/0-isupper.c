#include "main.h"

/**
 * _isupper -  Check for upper letter
 * @x: Number to check
 * Return: 1 for upper or 0 for others
 */

int _isuper(int x)
{
	if (x>= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
